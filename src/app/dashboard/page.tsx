"use client";

import { Overview } from "@/features/dashboard";

/** Dashboard — Visão geral (GET /store/dashboard). Rota protegida pelo layout. */
export default function DashboardPage() {
  return <Overview />;
}
